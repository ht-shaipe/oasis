[package]
name = "wasm-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ui-schema = { path = "../../ui-schema" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
