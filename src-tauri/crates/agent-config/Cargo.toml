[package]
edition = {workspace = true}
name = "oasis-agent-config"
version = {workspace = true}

[dependencies]
serde = {version = "1", features = ["derive"]}
serde_json = "1"
chrono = "0.4"
dirs-next = "2"
